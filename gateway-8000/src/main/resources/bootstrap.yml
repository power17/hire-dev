spring:
  cloud:
    nacos:
      config:
        server-addr: 192.168.237.128:8848
        file-extension: yaml
        extension-configs:
          - data-id: jwt_config.yaml
            group: DEFAULT_GROUP
            refresh: true

